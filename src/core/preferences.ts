import type { PetPreferences } from "./types";

export const DEFAULT_PREFERENCES: PetPreferences = {
  muted: false,
  paused: false,
  scale: 2,
  energy: "normal",
  clickThrough: true,
  launchAtLogin: false
};
