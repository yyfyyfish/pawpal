{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the PawPal pet window.",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
